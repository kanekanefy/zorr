# Patches

应用在 `upstream/gardn/` 之上的补丁,由 Dockerfile 在 build 阶段
按文件名顺序 `patch -p1` 应用。

| # | 文件 | 作用 |
|---|------|------|
| 0001 | `add-title-and-rebrand.patch` | HTML 加 `<title>zorr</title>`,loading 文案改 "zorr" |
| 0002 | `client-ws-url-from-location.patch` | 客户端 WS URL 从 `window.location` 动态推导(不依赖编译时常量,镜像跨域名可移植) |
| 0003 | `map-background-color.patch` | 地图底色 `0xff987d72`(棕) → `0xff2c8a3e`(绿) |
| 0004 | `op-mode.patch` | **Super Fun Mode v2(Phase 5A 调优)**:升级 /30、XP ×8、HP ×5、玩家伤害 ×20、怪伤害 ×0.6、掉率 ×8、密度 ×3、8 槽全开。目标:**60-90 分钟通关 + 战斗有紧张感**(从"秒怪秒升"调到"有挑战")。multiplier 集中在 `Shared/OpMode.hh`。**额外**:Death.cc 加 CPU-controlled flower(bot)死亡时的 tier-aware bonus drops 池(rookie/competitor/veteran 各 1-3 个,来自 Common→Mythic 三个稀有度池)。 |
| 0005 | `super-starter-loadout.patch` | **OP 起手套装**:初次 spawn(`camera.inventory[0] == kNone`)时,在 `Server/Spawn.cc::player_spawn()` 自动给玩家 8 槽塞:Heaviest / Stinger / Yggdrasil / Rose / Faster / Tricac / Triplet / Peas。死亡重生不影响(那时 inventory 已不空) |
| 0006 | `bot-system.patch` | **Bot 玩家系统(Phase 3 + 5B 强化 + 30 总数)**:新 `Server/Bot.{hh,cc}` + `Server/BotManager.{hh,cc}`,启动时 spawn **30 个** bot(20 rookie level 1-10 / 7 competitor level 30-50 / 3 veteran level 70-99)。每 tick 跑简单 FSM(wander/engage/flee/dead),用 spatial_hash 找最近 mob **或敌方 flower**(Phase 5B1:bots 现在会主动追打玩家;bots 之间因为同 NULL_ENTITY team 不互相攻击)。新 bot player 标记 `EntityFlags::kCPUControlled`(Phase 5B2:让 Death.cc 知道这是 bot,死了给 tier-pool bonus drops)。Rookie loadout 改成 Common+Unusual 混合(Phase 5B3:原全 Basic 导致死了什么都不掉,因为 Basic 永远不出 drop)。GameInstance::{init,tick} 接入,CMakeLists 新增 .cc。 |
| 0007 | `add-starfish.patch` | **首只新怪 Starfish**(Phase 4 MVP):验证加怪 pipeline。`MobID::kStarfish` 加进枚举尾,`MOB_DATA` 加 Unusual 海星条目(HP 40-60, 伤害 15, drops Faster/Salt/Rose/Yucca, stationary),Easy zone spawns 加 weight 50000(略低于 Ladybug),`Client/Assets/Mob.cc::draw_static_mob` 加 5 角星 Canvas2D 渲染(粉色 0xffe86e82 + breath 动画 + 中心高光点)。**gardn 所有素材都是程序绘制,没有图片** — Wiki 上的 PNG 不能直接用,得手译几何路径。 |
| 0008 | `mobs-jellyfish-mushroom-bubble.patch` | **Phase 6 三连击新怪**(参考 hornex sandbox 设计):**Jellyfish**(Rare, Hard zone, HP 80-140, dmg 25, 青色钟形+触手 bcurve_to 摆动, drop Faster/Stinger/Salt/Bubble/Light, weight 8000 = 罕见见到才稀奇);**Mushroom**(Common, Easy zone, HP 30-55, dmg 8 + 6dmg/4s poison_damage,**首次在 mob 侧用 poison attribute**, 红蘑菇白点造型, drop Rose/Leaf/Iris/Azalea, weight 30000);**BubbleMob**(Common, Easy zone, HP 1-8 玻璃大炮, dmg 1, weight 80000 = 新手刷 XP 用, 5 个 drop 槽塞 3 个 Bubble 给后续 craft 系统刷料)。所有新怪 stationary。`MobID` 枚举尾加 3 ID(注意 `kBubbleMob` 避免与 `PetalID::kBubble` 混淆),`MOB_DATA` 末尾追加 3 entry,`MAP_DATA` Easy + Hard 加 spawn weight,`Client/Assets/Mob.cc::draw_static_mob` 加 3 case(用 `partial_arc(x,y,r,start,end,anticlockwise)` 和 `bcurve_to` 真 API,**不是** Canvas2D 那个 `arc(...)`)。 |
| 0009 | `craft-inventory-lottery.patch` | **Phase 6 抽奖式 craft + 持久 inventory**(参考 hornex `02-inventory.png`/`03-craft.png` 设计,但适配 gardn '1 PetalID = 1 rarity' 架构):**新增 `Game::persistent_inventory[PetalID::kNumPetals]`**(终身拾起计数)+ **新增 `Game::try_craft(rarity)`**(消耗 5 个该 rarity 任意 petal,按 `[60,45,30,20,12,6,3]%` 成功率,成功 +1 随机次档 petal,失败销毁 1-4 个)。**持久化**走现成 `Client/Storage.cc` STORED macro,序列化为 `(id, uint32 count)` pairs。**+1 触发**在 `Client/Game.cc::update()` loadout-diff 处:新 petal 出现在 slot 即增计数。**UI(v1, 被 0010 替换)**:`Client/Ui/TitleScreen/Inventory.cc` 按 rarity 行+petal 列网格(数量用 `format_score` 显示 5k/5m),`Client/Ui/TitleScreen/Craft.cc` 每 rarity 一行 + Craft 5 按钮 + 5 秒结果反馈文本。两个 panel 通过 `Ui::Panel::{kInventory,kCraft}` 加进 enum,`MainScreen.cc::make_panel_buttons` 加 "Inventory"(紫)+ "Craft"(绿)按钮。CMakeLists 加 2 个新 .cc。**纯客户端**,无服务端改动,不影响 loadout 系统。 |
| 0010 | `craft-inventory-ingame-visibility.patch` | **Patch 0009 跟进:修 in-game 不可见 + Craft UI 重做**。**问题**:0009 把按钮放在 `make_panel_buttons`(title_ui_window),玩家 Play 进游戏后 title fade out → 按钮消失;Craft UI 是"每 rarity 一行 Craft 5"按钮,信息密度低,选不了具体 petal。**修复**:(1) 新 `Client/Ui/TitleScreen/MetaSidebar.cc` —— 左侧竖排两个图标按钮(紫色 bag = Inventory, 绿色 anvil = Craft),挂进 `other_ui_window`(无 gate, 永远渲染)。(2) Inventory + Craft panel 也从 title_ui_window 移到 other_ui_window, 去掉 `&& Game::should_render_title_ui()` 阀。(3) `make_panel_buttons` 删 Inventory/Craft 按钮(留 Settings/Petals/Mobs/Changelog)。(4) **Craft UI 重做**:新增 `Game::craft_slots[5]/craft_slot_count/craft_active_rarity` + `craft_slots_set_rarity/add_petal/reset` + `try_craft_from_slots()`。Panel 显示 5 个 slot + Reset 按钮、success rate + Craft 按钮、6 个 rarity tab(skip Unique)、grid 显示玩家在当前 rarity 拥有的 petals(点 cell 加进下一个空 slot)。(5) Inventory 改成真正的 6 列网格(rarity ASC, petal_id ASC),代替原 row-per-rarity。两个 button→state map (`g_craft_tab_map`/`g_craft_cell_map`) 都放 file scope 避免 anon-namespace `extern` 死链。 |
| 0011 | `bot-aggro-and-panel-clicks.patch` | **修两枚长尾 bug**。**Bug A:Inventory/Craft 图标点不动** —— 0010 把 sidebar+panels 挂进 `other_ui_window`,但 `Game::init()` 早就把这个 window 设了 `no_polling=1`(`Window::poll_events` / `Element::poll_events` 都 short-circuit),所以子树永远收不到鼠标事件,图标 render 但点不响应。**修复**:把 sidebar/inventory/craft 从 `other_ui_window` 撤下,改为同时挂进 `title_ui_window` 和 `game_ui_window`(各一份独立 Element 实例,因为 panel 必须在当前 visible/polled window 的树里才行)。`Ui::Panel::inventory/craft` globals 已无人读,双实例覆盖无害。**Bug B:bot 不打怪、围一堆卡住** —— `BotManager::alloc_bot_camera` 把 bot camera team 设成 `NULL_ENTITY`,而野怪 `Map::spawn_random_mob` 也用 `NULL_ENTITY` team,`Detection.cc:13` 同 team 短路让野怪 AI 把 bot 当友军不 aggro;同时 `Bot::tick` engage 分支每 tick 把 bot 朝最近 mob 全力加速,直接撞进 mob 卡住,四周怪越聚越多。**修复**:(1) `cam.set_team(cam.id)` —— 每个 bot 自有 team(和真人玩家一致),野怪正常 aggro 打 bot。(2) bot 目标搜索加 `EntityFlags::kCPUControlled` 过滤,避免 bot 互相当目标。(3) 新 `BotConfig::FIGHT_RANGE = 90.0f`,engage 距离够近就停加速(petal attack-orbit 是 r+100,90 单位让花瓣扫弧扫到目标又不会贴脸卡住)。 |
| 0012 | `panel-position-icons-feedback.patch` | **Patch 0011 跟进:UI 三连优化**。(1) **面板位置**:Inventory/Craft panel 原来 `v_justify=Bottom` 锚在左下角,直接和 `LevelBar` 经验条、loadout petal 槽叠在一起,而且 craft 反馈文字在 panel 末尾 → 在屏幕最底端被这些东西完全遮住。改成 `v_justify=Middle` + `elt->x=70`(刚好 sidebar 右侧),配合从左侧水平滑入的动画,完全清空底部区域。(2) **图标重画**:`draw_bag_icon` 从"圆角矩形+把手"升级成有两个肩带圈环+前置口袋的背包轮廓;`draw_anvil_icon` 函数留旧名但内容换成更通用的锤子(横粗锤头+短脖+垂直把手),32px 下可读性高很多(原 anvil 轮廓在小尺寸下糊成一团)。(3) **Craft 反馈大改**:原来 12pt 暗黄色文字塞在 panel 末尾用户根本看不见,以为合成系统不工作。改成两个高对比横幅(成功绿底白字、失败红底白字)各自独立 `should_render`,显示在 panel 标题正下方,字号 18pt,显示时长延长到 8 秒。成功显示 `✓ Crafted XXX (+1)`,失败显示 `✗ Failed — lost N petals`。Craft 系统本身机制(消耗→roll→产物加进 inventory)经核验是好的,问题只在反馈不可见。 |
| 0013 | `craft-feedback-timer-fix.patch` | **修 craft 反馈横幅闪一下就没的 bug**。Patch 0012 的 `RESULT_DISPLAY_SECONDS = 8.0` 和 `age = Game::timestamp - last_craft_timestamp` 比较,但 `Game::timestamp` 是 `requestAnimationFrame` 给的**毫秒**单位,所以横幅其实只显示 **8 毫秒**(不到 1 帧)——用户当然看不见。改成 `RESULT_DISPLAY_MS = 8000.0`,正确显示 8 秒。顺便升级:字号 18→20pt,成功横幅前新增一个 28×28 的 `LastResultPetalSprite` Element(`draw_loadout_background(ctx, last_craft_result)`)inline 显示刚合出来的 petal 图标,一眼能看出"我刚合出了啥"。 |
| 0014 | `inventory-grows-when-loadout-full.patch` | **修"loadout 满了之后 inventory 不再涨"的 bug**。原始 `Server/Process/Collision.cc::_pickup_drop` 找到第一个空 slot 就 equip + delete drop;**没空 slot 就直接 return**,drop 留在地上,玩家撞不动了。这导致 client 的 `persistent_inventory[id]++` 触发器(在 `Client/Game.cc::tick` 监听 `cached_loadout` 变化)永远不再 fire,inventory 看上去就停止增长了。**Server 改动**:_pickup_drop 移除"没空 slot 就 return"的早退,改成**永远 consume drop**(snap 到 player 位置,然后 request_delete)——就算 16 个 slot 全满 drop 也会消失。**Client 改动**:`Client/Game.cc::tick` 新增一段 pickup 检测,扫所有 `kDrop` 实体,凡是 `pending_delete=1` 且离本地玩家 80 units 以内的 drop,counter +1(用 `std::unordered_set<uint32_t>` 按 entity hash 去重,避免 4-tick deletion_animation 窗口内重复计数)。**副作用**:bot 也会"消耗"它走过的 drop,如果 bot 离本地玩家很近(<80 units)会被误算到玩家头上——v1 接受,边界情况。 |
| 0015 | `drag-inventory-to-loadout.patch` | **新增 inventory→loadout 拖拽装备**(用户最痛的一点:背包里有东西但装不上)。**协议**:`Shared/Binary.hh::Serverbound` 加 `kEquipFromInventory`,`Server/Client.cc` 加 handler(读 slot+petal_id,直接 `player.set_loadout_ids(slot, id)` + `slot.update_id` + `force_reload`,主排槽位才需重 spawn 副排自动同步;不做服务端 inventory 校验——和现有 kPetalSwap/kPetalDelete 一样信任 client,反正 inventory 状态只在 client 端持久化)。**Client 发送**:新 `Game::equip_from_inventory(pos, id)` 写包发出去。**拖拽 UI**:全局新增 `Game::inventory_drag_id : PetalID::T`(kNone = 没拖),`InventoryPetalCell::on_event(kMouseDown)` 若 `Game::alive() && persistent_inventory[id]>0` 就 set inventory_drag_id;`Game::tick` 末尾每帧检查若 inventory_drag_id != kNone 且鼠标刚抬(`mouse_buttons_released LeftMouse`),沿 `Ui::UiLoadout::petal_backgrounds[]` 找鼠标位置下最近的 loadout 槽(同 `LoadoutPetal.cc::find_viable_target` 几何),命中且 dyn pos 合法就 dynamic→static 映射(副排折叠 `MAX_SLOT_COUNT - loadout_count` 的 gap),本地 `persistent_inventory[new]--/[old]++` 立即更新展示,然后发包给服务端。死亡时清空拖拽态。v1 不画 follow-cursor ghost(可后续 0017 加),release-over-slot 已足够 discoverable。 |
| 0017 | `panel-close-button-drag-ghost.patch` | **UI 抛光**:(1) Inventory + Craft panel 标题行用 `HFlexContainer` 包装(`title 左 / X 右` 自动 stretch 到 parent 宽度),右侧加红色 24×24 close X 按钮,共享 handler `Ui::on_panel_close` 把 `panel_open = kNone`。(2) 拖拽 ghost:新 `InventoryDragGhost` Element,`should_render` 在 `Game::inventory_drag_id != kNone` 时为真,`animate` 每帧把 `x/y` snap 到鼠标位置(window-centered scale-adjusted 坐标),`on_render` 半透明(global_alpha 0.85)画该 petal 的 `draw_loadout_background`,挂在 `game_ui_window` 上;现在用户从背包拖东西时能直接看到 petal 跟着鼠标走,大大提升 discoverability。(3) `MetaSidebar.cc` 加 `#include <InGame/Loadout.hh>`(draw_loadout_background)和 `<Client/Input.hh>`(mouse_x)。`TitleScreen.hh` exports `make_panel_close_button` / `on_panel_close` / `make_inventory_drag_ghost`。 |
| 0018 | `add-worm-mob.patch` | **新怪 Worm**(蠕虫):分节式的"重型蜈蚣"。设计:`Rare` 稀有度,血量 120-180(比蜈蚣高 2.4 倍),damage 14,radius 40,segments 8,移动速度遵循默认,drops 是 Heavy/Rock/Heaviest/Peas/Leaf/Moon(中段稀有度有用的实用花瓣)。**改动**:(1) `Shared/StaticDefinitions.hh::MobID` 末尾加 `kWorm`。(2) `Shared/StaticData.cc::MOB_DATA` 末尾追加 entry。(3) `Shared/StaticData.hh` 在 Easy zone 加 1500 权重(罕见 sighting,新人看个新鲜),Hard zone 加 25000 权重(成为该区中坚怪物)。(4) `Client/Assets/Mob.cc` 加 draw case:复用蜈蚣的"两端帽+主体钟"骨架(分节系统按这个结构拼装),换土褐红色 (`0xff8b5a3c`) + 更大眼睛点,无触角(蠕虫没有,顺便省渲染)。决策原因:Hornet/Spider 在 upstream 已经存在且已有 draw + zone 配置,真正"新怪"成本最低的是 Worm——既能验证我们的 mob 添加流程(已被 patch 0007/0008 验证过),又用 Segmented 组件做出和现有"圆球怪"不同的视觉差异。 |
| 0019 | `fix-renderer-namespace.patch` | **修 patch 0017 编译错误**(0017 推上去 CI 红了,这条 fix-forward):`InventoryDragGhost` 的 `animate` lambda 和 `on_render` 用了 `Ui::Renderer`,但 `Renderer` 类在全局 namespace 不在 `Ui::`,导致 `no type named 'Renderer' in namespace 'Ui'`。改成 `Renderer`(无 namespace 限定)。两行修改,纯编译修复,无行为变化。 |
| 0020 | `bot-ai-overhaul.patch` | **bot 行为大改造**(用户反馈"bot 像雕像")。三类根因 + 系统化修复:(1) **fight 距离**:patch 0011 的 `FIGHT_RANGE=90` 太远,petal orbit 半径 ~r+100 在 90 距离只擦怪边缘,且 bot 停在那不动 → mob 慢慢走进来怼死它。改成**按 tier 分**:Rookie 110(胆小)/ Competitor 70 / Veteran 45(贴脸打)。(2) **engage 时不再原地呆站**:进入 fight range 后改成**绕圈 strafe**——计算与目标垂直的方向,按 `sin(camera.id * 0.7 + ai_tick * 0.05)` 摆动,STRAFE_AMPLITUDE=0.6 倍 PLAYER_ACCELERATION,bot 之间相位错开不同步。如果 strafe 漂出 1.2× fight_range 还会有向心 bias。**petal 实际命中提升明显**(扫弧覆盖目标全身),且视觉上 bot 在围着怪转圈。(3) **wander 行为更生动**:`WANDER_CHANGE_TICKS 5s → BASE 1s + JITTER 0..2s`(不同步、更频繁换目标),`WANDER_STOP_DIST 50→20`(到点立刻找下一个),到点后加 `IDLE_JITTER_ACCEL=0.3` 倍随机加速度(看上去像玩家在原地小幅调整位置)。(4) **per-tier vision**:Rookie 400 / Competitor 500 / Veteran 650,Vet 视野更广。(5) **respawn 冷却**:5s→2s,死亡后场上"空人"时间大幅减少。 |
| 0021 | `click-to-equip-and-drag-highlight.patch` | **修 inventory 装备 bug**(用户反馈"东西到背包后没法装备到槽里")。根本原因:patch 0015 只实现了 drag,但**多数用户本能反应是点击**不是拖拽——一点 → kMouseDown 设了 drag_id → 鼠标没动 → kClick 触发但 cell 没 handler → drag_id 卡死;用户以为没反应。**修复 1:click-to-equip**。新 `Game::equip_to_first_empty(id)` —— 先扫主排找第一个空槽,没有就扫副排,找到就 `equip_from_inventory(pos, id)` + 本地 inventory--。`InventoryPetalCell` 新增 `kClick` handler:有库存就调 equip_to_first_empty,然后清 drag_id 防止 release-detector 再 fire。drag 操作不变(用户仍可拖到指定槽)。**修复 2:drag 可见反馈**。Cell render 时若 `Game::inventory_drag_id == id`,临时把 `stroke_hsv` 从 0.85 提到 1.4 + `line_width` 从 2 提到 4,渲染完再恢复——拖中的源 cell 边框明显发亮,告诉用户"是的我抓到了"。再加上 patch 0017 的跟随鼠标 ghost,操作可视化完整。**未改**:server 端 handler 维持 patch 0015 的实现(`set_loadout_ids` + Flower.cc 自动 detect id 变化 → 下 tick `slot.update_id` spawn 新 petal,无需在 client.cc 显式调 update_id)。 |
| 0022 | `inventory-v2-single-source-of-truth.patch` | **背包系统 v2 重设计**(用户反馈"无限复制 + 装不上"+"补丁越打越乱,该重做")。**根因**:patch 0009 在 `Game::tick` 每帧扫 `cached_loadout` 看见任意 slot 从 X 变成非 None 的 Y 就 `persistent_inventory[Y]++`——这跟 patch 0014 的 drop 消失检测**双重计数**(每捡 1 个入账 +2),还把 patch 0021 的本地 equip 减法**完全冲掉**(server 同步 loadout 时再 +1 抵消)。**v2 规则**(写死在 README 和代码注释):`persistent_inventory[id]` = 你拥有但未装备的数量。**唯一 +1 源**:patch 0014 的 drop-disappear-near-player 检测;**唯一减/退源**:本地 equip / craft;**永不变动**:loadout sync / respawn / 死亡 / super starter 8 个 OP。**改动**:(1) `Client/Game.cc::tick` 删掉那段 cached_loadout-diff 增量(从 patch 0009 到现在的根问题),只保留 prev_cached_loadout 写入(向后兼容存档)。(2) `Game::equip_to_first_empty(id)` 升级:空主排 → 空副排 → **最低 rarity 主/副槽且严格低于新 petal 的 rarity**(防误降级,Mythic 不会被 Common 顶掉),被顶掉的旧 petal 自动退回 inventory。 |
| 0023 | `inventory-always-render-cells.patch` | **修 inventory 布局腐烂 bug**(用户截图:"Total petals owned" 文字夹在两排 cell 中间,Lotus/Cutter cell 渲染到 panel 外面)。**根因**:`Client/Ui/Container.cc::VContainer::refactor` 第 82 行 `if (!elt->visible) continue` —— 不可见的子元素**跳过位置更新**,`y` 字段保留旧值。当 cell 因 `persistent_inventory[id]` 从 0→正→0→正反复切换 `should_render` 时,row 的 `y` 没及时更新,后续 row 跟着错位。**修复**:cells 永远 render(`should_render` gate 整体移除),0-count 时改用 dim 灰色背景 + 18% 透明度的 petal sprite + 不显示 count 数字 + 无 tooltip。同样 row 级 should_render 也移除——所有 row 总是 render。**副作用 + 加成**:布局稳定了,且变成 Pokédex 风格——玩家看到完整收集册,知道还有哪些没找到。**配套调整**:(1) "Total petals owned" 文字从 grid 之后移到 grid 之前(作为副标题),不再受 grid 布局影响。(2) ScrollContainer 高度 380→500,容纳完整网格(~10 行 × 55px = 550,最多滚 1 行)。(3) 0-count cell 点击 / 悬停被 `Game::persistent_inventory[id] > 0` 守卫,不会误触发 equip 或 tooltip。 |
| 0024 | `qa-hooks-and-inventory-fit.patch` | **修 craft 产物在 inventory 看不到 bug + 加 QA hooks**(用户反馈"我 craft 出了 Legendary,inventory 里看不到")。**根因**:49 个 petal 6 列 ≈ 9 行,每行 64px,总 content height ~600px,但 patch 0023 的 ScrollContainer height 只 500——Legendary/Mythic/Unique 行**在折叠线以下**,且 scroll wheel 在 canvas 上不那么直观。**修复 1**:ScrollContainer 500→650,容下整个收藏册不用滚。**修复 2**:加 `Client/QaHooks.cc` —— 一组 `extern "C" EMSCRIPTEN_KEEPALIVE` 函数,emscripten 导出后 JS 可以直接 `Module._qa_*()` 调用,绕过 canvas 点击模拟。覆盖:`qa_open_inventory/craft/close_panels`,`qa_inventory_count(id)`,`qa_loadout_at(pos)`,`qa_equip(id)`,`qa_craft_set_rarity/add/reset/execute`,`qa_last_craft_result/success/destroyed`,`qa_alive`,`qa_score`,以及 `qa_dump_state()` 把全部状态写进 `window.__qa_state` JSON。**为啥**:行业 SOTA(King / Riot / Roblox / Supercell)做 canvas 游戏 QA 都是"暴露脚本 API + 测试直接调"——而不是模拟点击。我们之前让 QA 子代理去模拟 click 失败就是因为这条路本来就是错的。配套 `CMakeLists.txt`:加入 `QaHooks.cc` 到 SRCS 列表,`EXPORTED_FUNCTIONS` 加 18 个 `_qa_*` 符号。无 anti-cheat gate(这游戏没 anti-cheat,加 gate 也防不住 DevTools)。 |

## 应用方式

Dockerfile 里已经做了。手动应用:

```bash
cd upstream/gardn
for p in ../../patches/*.patch; do patch -p1 < "$p"; done
```

## 重新生成补丁

```bash
cd upstream/gardn
git diff > ../../patches/NNNN-description.patch
git checkout .   # 回退,补丁是唯一真相源
```
