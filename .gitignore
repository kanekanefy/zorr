# harness state
.claude/

# build output
build/
*.wasm
*.o
*.a

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# local env overrides
.env
.env.local

# node
node_modules/

# deploy script secrets
deploy/.secrets

# local research notebook — contains 1MB wasm + 392KB sprite atlas from
# another project (sandbox.hornex.pro). See its README.md for legal notes.
# Keep on disk only — not for git history.
docs/hornex-reference/

# scratch file (a previous suggestion-list, not for committing)
.gitignore.add
